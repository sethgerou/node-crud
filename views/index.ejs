<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>NODE CRUD - SETH</title>
  </head>
  <body>
    <h1>Seth's joke collection</h1>
        <a href="/quotes">quotes collection</a>

    <ul class=jokes>
      <% for (var i=0; i<jokes.length; i++) { %>
        <li class="joke">
          <h5><%= jokes[i].title %>:
          <%= jokes[i].joke %></h5>
        </li>
      <% } %>
    </ul>

  <h3>Please submit a joke</h3>
    <form action="/jokes" method="post">
      <input type="text" placeholder="title" name="title">
      <input type="text" name="joke" placeholder="your joke here">
      <button type="submit">Submit</button>
    </form>
  </body>
</html>
